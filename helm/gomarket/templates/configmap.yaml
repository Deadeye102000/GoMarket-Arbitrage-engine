apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gomarket.fullname" . }}-config
  labels:
    {{- include "gomarket.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
