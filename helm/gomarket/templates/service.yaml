apiVersion: v1
kind: Service
metadata:
  name: {{ include "gomarket.fullname" . }}
  labels:
    {{- include "gomarket.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gomarket.selectorLabels" . | nindent 4 }}
